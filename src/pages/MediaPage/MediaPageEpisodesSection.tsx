import React from "react";

interface Props {
  mediaId: number | null;
}

const MediaPageEpisodesSection = ({ mediaId }: Props) => {
  return <div>MediaPageEpisodesSection {mediaId} </div>;
};

export default MediaPageEpisodesSection;
